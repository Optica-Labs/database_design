# Legacy Schemas Reference

This directory is retained for historical/reference workflows.

## Primary Deployment

Use canonical create pipeline instead:

- `sql/create.sql`
- `sql/create/README.md`

## Legacy Files

- `schema_unified_complete.sql`
- `schema_ai_range_only.sql`
- `schema_peregrine_only.sql`

For current production creation and validation, use `sql/create/` and
`sql/variants/`.
